# Checkpoint — July 24, 2026

## State
Repo just created (graduated from `../solana-agent-exploration` after research → validation → PRD). No code yet.

- `PRD.md` — approved build spec (v1.0)
- `CLAUDE.md` — product principles, stack, scope, kill-switch
- `lessons/` — lessons-learned log (5 validation entries so far)
- `.superstack/idea-context.md` — phase handoff (Idea → Build), read by /scaffold-project

## Next steps
1. User spins up private GitHub remote and wires `origin`
2. `/scaffold-project` — Next.js app + keeper service + adapter module structure
3. Week 1 per PRD §5: Bags adapter → Pump.fun adapter → scanner UI → aggregate metrics job → Telegram alerts
4. In parallel: 5–10 creator interviews; log findings in `lessons/`
5. Kill-switch check end of week 1 (PRD §7)

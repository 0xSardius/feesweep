# Copy to .env and fill in. NEVER commit .env (gitignored).

# Helius — RPC + webhooks (accrual detection, aggregate scans)
HELIUS_API_KEY=

# PumpPortal creator-fee API (Pump.fun adapter)
PUMPPORTAL_API_KEY=

# Bags Public API v2 (Bags adapter)
BAGS_API_KEY=

# Postgres (local dev: docker compose; prod: Neon)
DATABASE_URL=postgres://feesweep:feesweep@localhost:5432/feesweep

# Telegram bot (read-only accrual alerts, week 1)
TELEGRAM_BOT_TOKEN=

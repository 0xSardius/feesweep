services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: feesweep
      POSTGRES_PASSWORD: feesweep
      POSTGRES_DB: feesweep
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
